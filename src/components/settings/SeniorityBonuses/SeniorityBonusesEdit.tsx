import React, { ReactElement } from 'react';

import SeniorityBonusesContainer from './SeniorityBonusesContainer';

const SeniorityBonusesEdit = (): ReactElement => {
  return <SeniorityBonusesContainer isEditable={true} />;
};

export default SeniorityBonusesEdit;
