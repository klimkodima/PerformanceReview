import { t } from 'i18next';

export const HEADER_CELLS: string[] = [
  t('teams.header_cell_teamName'),
  t('teams.header_cell_teamLeaderName')
];

export const ERRORS_TEXT = {
  emptyField: t('teams.empty_field')
};
