import { ReactElement } from 'react';

import TeamsContainer from './TeamsContainer';

const TeamsEdit = (): ReactElement => {
  return <TeamsContainer isEditable={true} />;
};

export default TeamsEdit;
