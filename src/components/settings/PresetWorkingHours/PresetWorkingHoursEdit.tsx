import { ReactElement } from 'react';

import PresetWorkingHoursContainer from './PresetWorkingHoursContainer';

const PresetWorkingHoursEdit = (): ReactElement => {
  return <PresetWorkingHoursContainer isEditable={true} />;
};

export default PresetWorkingHoursEdit;
