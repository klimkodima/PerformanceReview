import { t } from 'i18next';

import { ConstantsTextType } from '../../types';

export const BUTTONS: ConstantsTextType = {
  saveBtn: t('preset_working_hours_table.modal_save_button_text'),
  cancelBtn: t('preset_working_hours_table.modal_cancel_button_text')
};
