import { ReactElement } from 'react';

import PresetWorkingHoursContainer from './PresetWorkingHoursContainer';

const PresetWorkingHours = (): ReactElement => {
  return <PresetWorkingHoursContainer isEditable={false} />;
};

export default PresetWorkingHours;
