export type RowType = {
  classesContainer: string;
  classesLabel: string;
  classesText: string;
  label: string;
  text: string;
};

export type inputDataType = {
  key: string;
  value: string | number;
};

export type outputDataType = {
  label: string;
  text: string;
  order: number;
};
